PORT=3000
NODE_ENV=production
API_BASE_URL=http://SEU-IP-OU-DOMINIO:3000
CORS_ORIGINS=https://maniadealegria.shop,https://SEU-USUARIO.github.io,http://localhost:5500,http://127.0.0.1:5500
UPLOADS_DIR=/var/www/meusite/uploads/produtos
DB_PATH=/var/www/meusite/backend/database.sqlite

# === SEGURANÇA (MUDE ANTES DE PUBLICAR!) ===
# A senha usada para entrar no painel admin (admin.html)
ADMIN_PASSWORD=troque-esta-senha
# Token retornado pelo login; deve ser longo e aleatório (ex: gere com "openssl rand -hex 32")
ADMIN_TOKEN=troque-este-token-secreto
